webdriver {
  driver = chrome
}

chrome {
  options {
    args = [
      "--start-maximized",
      "--disable-notifications"
    ]
  }
}

serenity {
  project.name = "Serenity BDD Automation"
  take.screenshots = AFTER_EACH_STEP
  restart.browser.for.each = SCENARIO
}

accounts {
  ADMIN {
    username = "Admin"
    password = "admin123"
  }
}
